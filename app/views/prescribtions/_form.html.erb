<%= form_for(prescribtion) do |f| %>
  <% if prescribtion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(prescribtion.errors.count, "error") %> prohibited this prescribtion from being saved:</h2>

      <ul>
      <% prescribtion.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :patient_id %>
    <%= f.text_field :patient_id, class: 'form-control' %>
  </div>

  <div class="field">
    <%= f.label :doctor_id %>
    <%= f.text_field :doctor_id, class: 'form-control' %>
  </div>
  <div class="actions">
    <%= link_to "Medication", new_prescribtion_detail_path %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
