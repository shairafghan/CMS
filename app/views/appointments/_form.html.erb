<%= form_for(appointment) do |f| %>
  <% if appointment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(appointment.errors.count, "error") %> prohibited this appointment from being saved:</h2>

      <ul>
      <% appointment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :patient_id %>
    <%= f.text_field :patient_id %>
  </div>

  <div class="field">
    <%= f.label :doctor_id %>
    <%= f.text_field :doctor_id %>
  </div>

  <div class="field">
    <%= f.label :ap_date %>
    <%= f.date_select :ap_date %>
  </div>

  <div class="field">
    <%= f.label :ap_time %>
    <%= f.time_select :ap_time %>
  </div>

  <div class="field">
    <%= f.label :receptionist_id %>
    <%= f.text_field :receptionist_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
